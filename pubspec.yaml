name: minber_super_app_new_fixed
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

# CORRECTED: Use a standard, flexible SDK range
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Your original packages
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  fluttertoast: ^8.2.4
  http: ^1.5.0
  hijri: ^3.0.0
  flutter_qiblah: ^3.1.0
  sensors_plus: ^6.1.2
  geolocator: ^13.0.4
  geocoding: ^2.2.0
  adhan_dart: ^1.0.9
  intl: ^0.18.0
  timezone: ^0.9.0
  flutter_timezone: ^4.1.1
  url_launcher: ^6.3.2
  webview_flutter: ^4.13.0
  device_info_plus: ^12.1.0
  youtube_player_iframe: ^5.2.2
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  flutter_local_notifications: ^18.0.1
  video_player: ^2.10.0
  flutter_compass_v2: ^1.0.3
  permission_handler: ^11.3.1
  logger: ^2.6.2
  workmanager: ^0.9.0+3
  connectivity_plus: ^7.0.0
  provider: ^6.1.5+1
  chewie: ^1.13.0
  google_fonts: ^6.3.2
  pinput: ^5.0.2
  shimmer: ^3.0.0
  smooth_page_indicator: ^1.2.1
  flutter_staggered_animations: ^1.1.1

  # CORRECTED: Add better_player here
  # better_player: ^0.0.84

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.jpg"
  min_sdk_android: 21
